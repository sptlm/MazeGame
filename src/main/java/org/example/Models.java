package org.example;

public class Models {

    public static final String playerModel = Color.ANSI_RED + "X" + Color.ANSI_RESET;

    public static final String wallModel = Color.ANSI_CYAN + "\u2588" + Color.ANSI_RESET;

    public static final String finishModel = Color.ANSI_YELLOW + "A" + Color.ANSI_RESET;

    public static final String passModel = Color.ANSI_GREEN + "o" + Color.ANSI_RESET;

    public static final String voidModel = Color.ANSI_WHITE + "." + Color.ANSI_RESET;
}
